import React from 'react';
import { Link } from 'react-dom';

export default () => {
    return (
        <div>
            I'm some other page!
            <Link to='/'>Go Back Home</Link>
        </div>
    );
};